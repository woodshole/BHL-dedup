!!!
%html{html_attrs}
  %head
    = javascript_include_tag 'gbsclasses.js'
    %title Deduplication Tool
    %meta{'http-equiv' => 'Content-Type', :content => 'text/html; charset=utf-8'}/
    = stylesheets
  %body
    #content
      = yield :page_header
      = render :partial=>'layouts/login_information' if logged_in?
      = render :partial=>'layouts/flash_notice' if flash[:notice]
      = yield :layout